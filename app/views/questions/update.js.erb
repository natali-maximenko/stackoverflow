$('.question-<%= @question.id %>-errors').html('<%= render 'layouts/errors', resource: @question %>');

<% if !@question.errors.present? %>
  $('.question_<%= @question.id %>').html('<%= j render @question %>');
<% end %>
